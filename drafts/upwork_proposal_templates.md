# Upwork Proposal Templates (3 verticals)

> **DRAFT.** Customize per job: open with a specific reference to the client's post, put the
> demo link in the first 3 lines, ask 1–2 technical questions, state timeline + rate.
> Placeholders in <ANGLE BRACKETS>. Don't blast generic — tailor the first sentence.

---

## Template 1 — RAG / AI Chatbot

Hi <NAME>, you're looking for <RESTATE THEIR NEED: e.g., "a RAG assistant over your internal
docs with citations">. I build exactly this in production.

Quick proof: here's a live 9-persona RAG copilot I built — <DEMO URL> (3-min walkthrough:
<LOOM>). It does role-scoped retrieval (each role only sees its own data), streaming answers
with source citations, hybrid retrieval (dense + BM25 + reranker), and GraphRAG-lite for
multi-hop questions. Code: github.com/Yacine-ai-tech/IntelAI.

Two questions so I can scope accurately:
1. What's the source corpus (size, format, update cadence)?
2. Do you need role-based access control over the data, or a single audience?

I can deliver a working pilot in <N> days. My rate is <RATE>/hr. Happy to start with a small
paid scoping task to de-risk.

— Yacine

---

## Template 2 — FastAPI / Python Backend

Hi <NAME>, for <RESTATE: "the async FastAPI service with auth and Postgres"> — that's my core
stack. Recent example: a 68-endpoint FastAPI backend (JWT + RBAC, WebSocket streaming,
Postgres, Prometheus `/metrics`, 40+ tests, Dockerized, one-command cloud deploy):
<DEMO URL> · github.com/Yacine-ai-tech/IntelAI.

Questions:
1. Is there an existing schema/OpenAPI spec, or green-field?
2. Auth model — JWT/OAuth/SSO, and do you need role-based permissions?

Timeline: <N> days for the first vertical slice (endpoints + tests + CI). Rate <RATE>/hr.

— Yacine

---

## Template 3 — BI / Analytics / KPI Dashboard

Hi <NAME>, you need <RESTATE: "a KPI dashboard with forecasting">. I built a multi-domain
analytics platform (Finance, HR, IT, Ops, Logistics, ESG) with Recharts dashboards, Monte
Carlo forecasting with confidence intervals, anomaly detection, and board-ready PDF exports —
plus an AI copilot to ask questions in plain English: <DEMO URL>.

Questions:
1. Where does the data live today (warehouse, spreadsheets, an API)?
2. Which 5–8 KPIs matter most for v1, and who are the audiences (exec vs ops)?

I can ship a working dashboard on your real metrics in <N> days. Rate <RATE>/hr.

— Yacine

---

### Logging (Notion/sheet)
date · job title · niche · template · demo link sent · client country · client reviews · outcome
