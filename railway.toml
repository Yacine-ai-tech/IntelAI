[build]
builder = "DOCKERFILE"

[deploy]
# The Dockerfile CMD binds $PORT (uvloop/httptools-tuned); no startCommand override needed.
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
