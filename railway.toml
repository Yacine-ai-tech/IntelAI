# Railway deploy descriptor for IntelAI. The Dockerfile CMD binds $PORT.
# Startup loads embedding models + syncs users across regions, so allow a long healthcheck window.
[build]
builder = "DOCKERFILE"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 600
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
